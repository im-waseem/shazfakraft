# Auto-generated by scripts/deploy.sh — DO NOT edit manually.
# Overwritten on every deploy to point nginx at the active slot.
# Prefixed 00- so nginx loads it before shazfakraft.conf (alphabetical order).
upstream nextjs_app {
    server shazfakraft-blue:3000;
    keepalive 32;
}
